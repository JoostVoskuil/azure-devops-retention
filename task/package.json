{
  "name": "tagging",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-rel": "npm install --production && tsc index.ts",
    "build-test": "npm install && tsc",
    "clean": "rm -rf ./node_modules/ && rm -rf ./coverage/ && rm -rf ./.nyc_output/ && rm -rf test-results.xml && find . -name '*.js' -delete && find . -name '*.js.map' -delete && rm -f package-lock.json && find . -name '.taskkey' -delete",
    "lint": "eslint . --ext .ts",
    "test": "nyc mocha tests/_suite.js --timeout 10000 && rm -rf ./.nyc_output/",
    "test-ci": "nyc --reporter=cobertura --reporter=html mocha tests/_suite.js --reporter mocha-junit-reporter --reporter-options mochaFile=./test-results.xml --timeout 10000"
  },
  "nyc": {
    "extension": [
      ".ts",
      ".tsx"
    ],
    "exclude": [
      "**/*.d.ts",
      "**/tests/*.ts"
    ],
    "all": true
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/JoostVoskuil/azure-devops-retention"
  },
  "author": "joost@foxhole.nl (joost@foxhole.nl)",
  "license": "ISC",
  "dependencies": {
    "azure-pipelines-task-lib": "^5.2.1",
    "azure-devops-node-api": "^15.1.1",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@typescript-eslint/eslint-plugin": "^8.41.0",
    "@typescript-eslint/parser": "^8.41.0",
    "eslint": "^9.34.0",
    "mocha": "^11.7.1",
    "mocha-junit-reporter": "^2.2.1",
    "nyc": "^17.1.0",
    "source-map-support": "^0.5.21",
    "sync-request": "^6.1.0",
    "typescript": "^5.9.2"
  }
}
